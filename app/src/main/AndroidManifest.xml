<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.projecttimesheet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".android.app.user.LoginActivity"
            android:configChanges="${configChanges}">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".android.app.user.ForgotPasswordActivity"
            android:configChanges="${configChanges}"
            android:screenOrientation="portrait" />
        <activity
            android:name=".android.app.user.SignUpActivity"
            android:configChanges="${configChanges}"
            android:screenOrientation="portrait" />
        <activity
            android:name=".android.app.MainActivity"
            android:configChanges="${configChanges}"
            android:screenOrientation="portrait" />
    </application>

</manifest>